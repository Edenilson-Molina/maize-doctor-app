/**
 * Stub para assets binarios que Metro resuelve como modulos nativos (require devuelve
 * un id numerico) pero que Jest no puede parsear como JS. Mismo valor que usa el
 * transformer de assets de jest-expo para el resto de extensiones registradas en Metro.
 */
module.exports = 1;
